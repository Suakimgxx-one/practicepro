# analysis_engine

Standalone, framework-free Python package. See individual modules for
alignment (M5), pitch (M6), rhythm (M7), tempo (M8), dynamics (M9),
feedback (M10). Orchestration lives in worker/tasks/analyze.py.
