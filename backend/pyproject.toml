[tool.pytest.ini_options]
testpaths = ["tests", "analysis_engine/tests"]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "session"
pythonpath = ["."]

[tool.black]
line-length = 100

[tool.ruff]
line-length = 100
